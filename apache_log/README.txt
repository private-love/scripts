#APACHELOG脚本
#version:1.0
#功能：
	* 每天定时去各服务器获取昨天的apache日志大小信息
	* 如果(SIP,DOMAIN)唯一性下能插入，则表示为新域名，则增加时间，不能插入则表示旧
#参数说明：
	* -u [2016-11-26]:默认更新昨天的日志信息，可增加日期参数指定更新日期，但是会DOMAIN的LTIME还是为昨天时间
	* -d:发送所有domain信息
	* -di:发送domain昨天进行的增删信息
